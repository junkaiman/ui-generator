export enum PreviewerTabs {
  Preview = "preview",
  Code = "code",
}

export enum MobileTabs {
  ChatInterface = "chat-interface",
  Preview = "preview",
}

/**
 * DB related enums
 */
export enum DBNames {
  Chats = "chats-database",
}
